my_list = [1, 2, 3]
print(my_list)

myset = set()

type(myset)

class Dog(): # class keyword

    def __init__(self, breed, name, spots):

        # Attributes
        # We take in the argument
        # Assign it using self.attribute_name
        self.breed = breed
        self.name = name
        self.spots = spots

my_dog = Dog(mybreed='Puddle', name='Niño', spots=False)
print(my_dog) 
